{
  "name": "kunstpunkte-map",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run check && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "data:import": "node scripts/import-data.ts",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "entities": "8.1.0",
    "json5": "2.2.3",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
